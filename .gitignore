# Dependencias
node_modules/

# Build
# build/ — incluida intencionalmente para deploy en cPanel sin CI/CD
.svelte-kit/

# Variables de entorno (nunca commitear)
.env
.env.local
.env.production

# Base de datos local (si algún dev usa SQLite en local)
*.db
*.db-journal

# Archivos del sistema
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*

# Uploads (documentos subidos por usuarios — no van al repo)
uploads/
